spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none

app:
  scraper:
    mercadona:
      base-url: https://tienda.mercadona.es/api
      request-timeout: 10s
  scheduler:
    sync:
      enabled: false

