-- V1__init.sql
-- Baseline migration. The schema starts empty; subsequent migrations add tables.
-- This migration intentionally contains no DDL statements so that Flyway
-- can establish its baseline on a fresh database.

SELECT 1;

